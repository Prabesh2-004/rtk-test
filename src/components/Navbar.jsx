import React from "react";
import { Link } from "react-router";
import { Plus } from 'lucide-react';

const Navbar = () => {
  return (
    <div>
      {/* <header className="mb-2 px-4 shadow">
        <div className="relative mx-auto flex max-w-screen-lg flex-col py-4 sm:flex-row sm:items-center sm:justify-between">
          <h2 className="text-blue-600 font-bold text-2xl">ToDoList</h2>
          <button className=" flex p-3 px-5 font-bold border rounded-xl text-blue-600 border-blue-600 hover:bg-blue-600 hover:text-white">Create <Plus /></button>
        </div>
      </header> */}
    </div>
  );
};

export default Navbar;
